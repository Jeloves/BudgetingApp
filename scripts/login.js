document.querySelector('#btn_validate_credentials').addEventListener('click',validateCredentials())

function signInUser(username,password) {
    if (username === null || password === null) {

    } else {
        
    }
}